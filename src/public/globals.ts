export const Camera = {
  integer_approximation: true
}

export const Frame = {
  integer_approximation: {
    frame: false,
    canvas: true
  }
}

export const globals = {
  camera: Camera,
  frame: Frame
}

export default globals
